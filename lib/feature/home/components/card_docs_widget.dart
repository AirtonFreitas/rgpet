import 'package:flutter/material.dart';

class CardDocumentos extends StatelessWidget {
  const CardDocumentos({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Row(
      children: [
    Image.asset(
      'name',
      width: 60,
      height: 60,
    ),
      ],
    );
  }
}
